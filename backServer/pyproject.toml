[project]
name = "qwen-chat-server"
version = "0.1.0"
description = "Local Qwen3-4B chat API with SSE streaming"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "transformers>=4.51.0",
    "accelerate>=1.0.0",
    "torch>=2.5.0",
    "pydantic>=2.0.0",
]

[tool.uv]
dev-dependencies = []
